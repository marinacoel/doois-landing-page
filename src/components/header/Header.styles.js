import { makeStyles } from '@mui/styles'

export const headerStyles = makeStyles(() => ({
    lp_header: {
      boxSizing: 'border-box',
  
      width: '100%',
      minHeight: '50vh',

      backgroundColor: '#000',
  
      display: 'flex',
      alignItems: 'center',
    }
  }))